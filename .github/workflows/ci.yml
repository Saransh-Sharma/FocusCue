name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4

      - name: Build App Store Debug
        run: |
          xcodebuild build \
            -project FocusCue.xcodeproj \
            -scheme FocusCue \
            -configuration Debug \
            -destination "platform=macOS" \
            CODE_SIGNING_ALLOWED=NO \
            -quiet

      - name: Build Direct Debug
        run: |
          xcodebuild build \
            -project FocusCue.xcodeproj \
            -scheme "FocusCue Direct Debug" \
            -configuration DirectDebug \
            -destination "platform=macOS" \
            CODE_SIGNING_ALLOWED=NO \
            -quiet
